import React from 'react';


interface IOwnProps {
  readonly [key: string]: any;
}
export const SignUpPage = (props: IOwnProps) => {
  return (
    <div className={'app-layout__page'}>
      SignUpPage works, но это не точно.
    </div>
  );
};
