import React from 'react';


interface IOwnProps {
  readonly [key: string]: any;
}
export const SignInPage = (props: IOwnProps) => {
  return (
    <div className={'app-layout__page'}>
      SignInPage works, но это не точно.
    </div>
  );
};
